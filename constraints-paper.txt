# Exact versions the paper's numbers are produced with. The package itself declares
# ranges (pyproject.toml); this file pins them for the reproducibility run:
#
#     python -m pip install -e ".[dev]" -c constraints-paper.txt
#
# Update it only together with a re-run of every experiment. three.js is vendored at
# r169 under web/vendor and is not installed by pip.
numpy==2.4.6
manifold3d==3.5.3
pytest==9.1.1
